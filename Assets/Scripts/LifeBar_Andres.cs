using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LifeBar_Andres : MonoBehaviour
{
    //GameObject indicador;
    // Start is called before the first frame update
    void Start()
    {
     //   indicador = GameObject.Find("LifeBar");
       // Debug.Log(indicador.GetComponent<Transform>().position.x);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
